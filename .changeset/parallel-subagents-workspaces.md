---
"deeptask": minor
---

    Add a global folder, workspace, and conversation sidebar. Occupied workspaces automatically create a sibling git worktree and move the later conversation there. Non-git folders initialize automatically, workspace names stay as typed, and history is grouped by workspace for the current folder. Existing workspaces reload when a window opens. Deleting a workspace can keep conversations under main or delete them all.
