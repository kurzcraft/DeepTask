---
"kilo-code": patch
---

Run Windows cmd.exe commands through a child-process fallback with full output and exit codes, mirror that fallback execution live in the integrated terminal through a pseudoterminal transcript (command line, streamed output, exit code, aborts), degrade Windows shell-integration timeouts instead of failing, require long or hang-prone commands to be scripted first, always deliver edited messages instead of dropping them silently, keep the green completion summary visible when continuing a finished task, add a manage_provider_profile tool so the model can list, create, update, and rename provider profiles and switch its own reasoning effort including off, auto-approve every manage_provider_profile action under the default-on profile-switch gate, sync reasoning-effort changes on the active profile immediately to the webview and the next request, and deliver an immediate reply after task completion as a continuation turn so the model answers the user instead of re-ending with an empty summary.
